# Vite exposes vars prefixed with VITE_ to client code.
# The API is proxied via vite.config.ts, so no VITE_API_URL is needed for dev.
# These are for reference/documentation only.

# Backend API port (used in vite.config.ts proxy target)
# VITE_API_PORT=6000
